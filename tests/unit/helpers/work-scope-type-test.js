
import { workScopeType } from 'ember-quickstart/helpers/work-scope-type';
import { module, test } from 'qunit';

module('Unit | Helper | work scope type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = workScopeType([42]);
  assert.ok(result);
});

